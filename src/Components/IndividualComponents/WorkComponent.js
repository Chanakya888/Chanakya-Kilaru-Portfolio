import React from "react";

export const WorkComponent = (props) => {
  return (
    <div className="Return-command-container">
      <p>
        Following are handpicked works by our agent apart from saving the
        galaxy, you can type them in the command centre or tap on the suggested
        commands to know more.
      </p>
    </div>
  );
};
